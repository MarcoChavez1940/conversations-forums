<ul class="navbar-nav mr-auto">
    <% if current_user %>
        <li>
            <%= link_to "nueva sala", new_chatroom_path%>
        </li>
        &emsp;
            <li>
                <%= link_to "salas de chat", chatrooms_path %>
            </li>
            <% end %>
</ul>

<% if current_user %>
    <h5 name="<%=current_user.username%>">
        ¡Bienvenido
        <%= current_user.username %>!
    </h5>
    <% end %>
&emsp;
        <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
                <li>
                    <%= link_to "Log Out", logout_path, method: "delete" %>
                </li>
                <% else %>
                <li>
                    <%= link_to "Log In", login_path %>
                </li>
                &emsp;
                    <li>
                        <%= link_to "Sign Up", signup_path %>
                    </li>
                    <% end %>
        </ul>